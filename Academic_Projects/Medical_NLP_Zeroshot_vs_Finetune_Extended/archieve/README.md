# Archive

Legacy experiments, including **T5-based zero-shot** notebooks (`T5_zeroshot_*`). Superseded in v2 by `GPT4_zeroshot_*` and the `zeroshot/` package (GPT-4 / Mistral-7B-Instruct per the updated paper).
This is a trial repo for 266 final project
